{
  "name": "import-dashboard",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "back",
    "client"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-back": "cd back && npm run build",
    "start-back": "cd back && npm run start",
    "start-back:developpment": "cd back && npm run start-developpment",
    "start-client": "cd client && npm run dev",
    "build-client": "cd client && npm run build",
    "start-all": "concurrently \"npm run start-back\" \"npm run start-client\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
